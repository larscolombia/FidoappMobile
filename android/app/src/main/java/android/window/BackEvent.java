package android.window;

/**
 * Clase stub para BackEvent.
 * Esta implementación no tiene funcionalidad, solo existe para que R8 encuentre
 * la clase durante la compilación.
 */
public class BackEvent {
  // Puedes dejarla vacía o agregar un constructor vacío
  public BackEvent() {
  }
}
